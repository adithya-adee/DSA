#ifndef SORTING_ALGOS
#define SORTING_ALGOS

#include <vector>
void merge_sort(int low, int high, std::vector<int> nums) {
  if (low >= high)
    return;

  int mid = (high + low) / 2;
}

void quick_sort() {}

#endif // !SORTING_ALGOS
